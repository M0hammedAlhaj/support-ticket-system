spring:
  application:
    name: user-service-test
  
  data:
    mongodb:
      host: localhost
      port: 27017
      database: testdb
      
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

grpc:
  server:
    port: 0  # Random port for tests

logging:
  level:
    root: INFO
    com.ticket.userservice: DEBUG
